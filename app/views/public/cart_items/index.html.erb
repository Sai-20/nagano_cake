<h1>ショッピングカート</h1>
<table>
  <th><%= link_to "カートを空にする", public_destroy_all_path, method: :delete %></th>
  <tr>
    <th>  商品名  </th>
    <th>  単価  </th>
    <th>  数量  </th>
    <th>  小計  </th>
  </tr>
  <% @cart_items.each do |cart_item| %>
  <tr>
    <td><%= cart_item.item.name %></td>
    <td><%= cart_item.item.with_tax_price %></td>
    <td><%= cart_item.amount%></td>
    <td><%= cart_item.subtotal %></td>
    <td><%= link_to "削除", public_cart_item_path(cart_item.id), method: :delete %></td>
  </tr>
  <% @total = @total + cart_item.subtotal %>
  <%# @total += cart_item.subtotal 短い文にできる %>
  <% end %>
</table>
<table>

  <tr>
    <th>合計金額</th>
    <td><%= @total %></td>
  </tr>

</table>
  <p><%= link_to "買い物を続ける",  public_homes_top_path %></p>
  <p><%= link_to "情報入力に進む",  new_public_order_path %></p>